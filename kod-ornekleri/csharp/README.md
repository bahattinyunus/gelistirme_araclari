# C# Kod Örnekleri

Bu klasör, C# ile yazılım geliştirme araçlarının kullanımını gösteren örnekler içerir.

## 📁 Dosyalar

- `TodoApp.cs` - C# class yapısı ile Todo uygulaması
- `TodoAppTests.cs` - xUnit ile test örnekleri
- `TodoApp.csproj` - .NET proje dosyası

## 🚀 Kullanım

### 1. .NET SDK Kurulumu

.NET 6.0 veya üzeri gereklidir.

### 2. Projeyi Çalıştırma

```bash
# Projeyi derle ve çalıştır
dotnet run

# Sadece derle
dotnet build

# Testleri çalıştır
dotnet test
```

### 3. Visual Studio ile

- Visual Studio 2022'de projeyi açın
- `TodoApp.cs` dosyasındaki `Main` metodunu çalıştırın
- Test Explorer'dan testleri çalıştırabilirsiniz

## 🛠️ Kullanılan Araçlar

- **.NET SDK** - C# derleyici ve runtime
- **NuGet** - Paket yönetimi
- **xUnit** - Test framework
- **Git** - Sürüm kontrolü

## 📝 Git Kullanımı

```bash
git add kod-ornekleri/csharp/
git commit -m "C# örnekleri eklendi"
git push origin main
```

## 💡 Öğrenilen Konular

1. C# class ve object yapısı
2. Properties kullanımı
3. LINQ sorguları
4. xUnit ile test yazma
5. Exception handling
6. .NET proje yapısı

## 📦 NuGet Komutları

```bash
dotnet restore    # Paketleri yükle
dotnet build      # Derle
dotnet test       # Test çalıştır
dotnet run        # Çalıştır
dotnet publish    # Yayınla
```

