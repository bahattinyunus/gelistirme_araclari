# Git Örnek Akışı

1. Yeni repo oluştur: `git init`
2. Uzak repo ekle: `git remote add origin <URL>`
3. Değişiklik yap ve duruma bak: `git status`
4. Dosyaları ekle: `git add .`
5. Commit oluştur: `git commit -m "ilk commit"`
6. Main branch'e push et: `git push -u origin main`
